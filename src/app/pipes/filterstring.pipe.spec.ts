import { FilterstringPipe } from './filterstring.pipe';

describe('FilterstringPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterstringPipe();
    expect(pipe).toBeTruthy();
  });
});
