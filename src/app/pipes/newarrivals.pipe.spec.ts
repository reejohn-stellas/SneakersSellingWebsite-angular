import { NewarrivalsPipe } from './newarrivals.pipe';

describe('NewarrivalsPipe', () => {
  it('create an instance', () => {
    const pipe = new NewarrivalsPipe();
    expect(pipe).toBeTruthy();
  });
});
